#include <stdio.h>

/**
 * main - Entry point.
 *
 * Description: a program that prints
 * @argc: the count of the arguments
 * @argv: the Arguments
 * Return: Always (0).
 */

int main(int argc, char const *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);

	return (0);
}
